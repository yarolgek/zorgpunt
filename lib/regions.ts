/** Shared regional messaging — keep UX copy consistent site-wide. */
export const activeRegionLabel = "Limburg, Noord-Brabant en Gelderland"

/** Tighter label where space is limited (e.g. sector section). */
export const activeRegionShort = "Limburg, Brabant en Gelderland"

export const activeProvinces = [
  "Limburg",
  "Noord-Brabant",
  "Gelderland",
] as const

/** For zzp'ers / opdrachtgevers outside the current service area. */
export const expansionNote =
  "We breiden onze dekking stap voor stap uit. Woont u verder weg of bent u zzp'er buiten onze regio? Meld u gerust aan — wij bewaren uw gegevens en nemen contact op zodra we daar actief zijn."

export const networkStats = {
  activeProfessionals: "400+",
  activeLabel: "Actieve zzp'ers, vandaag inzetbaar",
} as const
